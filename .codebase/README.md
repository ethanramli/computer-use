# .codebase — OS-Level Application Source

OS-level desktop controller lives here for any OS that installs this repo. Repo root holds planning (`.wiki/`, `README.md`, `AGENTS.md`).

Run app commands from this directory:

```bash
cd .codebase
python -m pytest
python -m mypy desktop
./bin/desktop --help
```
